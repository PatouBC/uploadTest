{% extends 'base.html.twig' %}

{% block title %}Tasklist{% endblock %}

{% block body %}
    <h1>Tasklist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tasklist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tasklist.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tasklist_index') }}">back to list</a>

    <a href="{{ path('tasklist_edit', {'id': tasklist.id}) }}">edit</a>

    {{ include('tasklist/_delete_form.html.twig') }}
{% endblock %}
