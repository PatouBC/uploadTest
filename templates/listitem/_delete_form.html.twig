<form method="post" action="{{ path('listitem_delete', {'id': listitem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listitem.id) }}">
    <button class="btn">Delete</button>
</form>
